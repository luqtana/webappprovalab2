@model IEnumerable<WebAppProvaLab2.Models.Profissao>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descricao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CBO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descricao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CBO)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProfissaoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProfissaoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProfissaoId })
        </td>
    </tr>
}

</table>
